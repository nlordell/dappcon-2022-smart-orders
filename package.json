{
  "name": "smart-order",
  "private": true,
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network goerli",
    "place-order": "hardhat run scripts/placeOrder.js --network goerli",
    "cancel-order": "hardhat run scripts/cancelOrder.js --network goerli",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.11.1",
    "node-fetch": "^2.6.7"
  }
}
